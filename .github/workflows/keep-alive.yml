name: Keep Render Alive

on:
  schedule:
    - cron: "*/5 0-16 * * *"   # 07:00 – 23:55 giờ Việt Nam
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping backend Render
        run: |
          curl -s https://interior-affiliate-backend.onrender.com/api/health
